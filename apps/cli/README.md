# Delineator CLI

Command-line interface for the [`delineator` package](/home/sam/Projects/Curatores%20Viarum/packages/delineator).

This app owns:

- argument parsing
- CLI exit codes
- user-facing command examples

The hydrology, parsing, geometry, and report-generation logic remain in the shared package.
